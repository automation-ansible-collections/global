---
controller_credentials:
  - name: "{{ orgs }} {{ env }} machine_ansible_user_credentials"
    description: "machine_ansible_user_credentials"
    credential_type: "Machine"
    organization: "{{ orgs }}"
    inputs:
      username: "user-ansible"
      password: "password"
...
