---
controller_credentials:
  - name: "{{ orgs }} {{ env }} aap_vault_credentials"
    description: "aap_vault_credentials"
    credential_type: "Vault"
    organization: "{{ orgs }}"
    inputs:
      vault_password: "password"
...
